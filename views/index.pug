extends layout

block content
  h1 Magical URL Shortener
  form(name="addurl" method="POST" action="/addurl" onsubmit="validateForm(long_url)")
    input#userUrl(type="text" placeholder="URL to Shorten" name="long_url")
    button.btn#btnSubmit(type="submit" disabled) Submit
  script(src='../javascripts/urlshortener.js')
